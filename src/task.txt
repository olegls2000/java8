* Созайде класс com.shape.Circle, у которого будет одно поле: int radius. В классе создайте два конструктора.
 Первый без параметров, в котором радиус генерируется случайно из интервала 20...100,
  второй в котором радиус передается через параметр.
  Создайте в классе метод (не статик!!!) который считает (возвращает) периметр (длину окружности).
  Создайте в классе метод (не статик!!!) который считает (возвращает) площадь круга.


* создайте метод который создает массив обьектов класса com.shape.Circle определенной длины.
Длина массива передается в параметр метода

** Созайде класс com.shape.Square(квадрат), у которого будет одно поле: int side.
В классе создайте два конструктора. Первый без параметров,
в котором side генерируется случайно из интервала 20...100, второй в котором side передается через параметр.
Создайте в классе метод (не статик!!!) который считает (возвращает) периметр.
Создайте в классе метод (не статик!!!) который считает (возвращает) площадь квадрата.

